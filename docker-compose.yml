services:
  api:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - db_data:/data
    environment:
      - SQLALCHEMY_DATABASE_URL=sqlite:////data/ip_lookup.db
    restart: unless-stopped

volumes:
  db_data:
